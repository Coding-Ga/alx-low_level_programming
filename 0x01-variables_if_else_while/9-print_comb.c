#include <stdio.h>
#include <unistd.h>
/**
 * main - This is the main function
 * Description: prints or outputs all possible combs of single-digit numbers
 * Return: Gives a value of 0
 */

int main(void)
{
int i;

for (i = '0'; i <= '9' ; i++)
{
putchar(c);

if (i != '9')
{
putchar(',');
putchar(' ');
}

}
return (0);
}
